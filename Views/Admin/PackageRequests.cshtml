@*  @model online_sms.Models.PackageRequest;

    @{
        ViewBag.Title = "Package Requests";
    }

    <h2>Package Requests</h2>

<table>
    <thead>
        <tr>
            <th>User ID</th>
            <th>Package Type</th>
            <th>Request Date</th>
            <th>Status</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            @foreach (var request in Model)
            {
                <tr>
                    <td>@request.UserId</td>
                    <td>@request.PackageType</td>
                    <td>@request.RequestDate.ToString("yyyy-MM-dd")</td> <!-- Formatting date -->
                    <td>@request.Status</td>
                    <td>
                        <a href="@Url.Action("ApproveRequest", new { id = request.RequestId })">Approve</a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="5">No requests found.</td> <!-- Message when no requests are available -->
            </tr>
        }
    </tbody>
</table>
 *@